
// import Notification from './Notification';
// import './NotificationList.css';

// const NotificationList = (notifications, markAsRead, markAllAsRead ) => {
//   return (
//     <div className="notification-list">
//       <header>
//         <h1>Notifications <span>({notifications.filter(n => !n.read).length})</span></h1>
//         <button onClick={markAllAsRead}>Mark all as read</button>
//       </header>
//       <ul>
//         {notifications.map(notification => (
//           <Notification key={notification.id} notification={notification} markAsRead={markAsRead} />
//         ))}
//       </ul>
//     </div>
//   );
// };

// export default NotificationList;